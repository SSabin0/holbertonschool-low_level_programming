#include "main.h"

/**
* print_most_numbers -  prints the numbers except 2 and 4.
*/

void print_most_numbers(void)
{

char c;

for (i = '0'; i < '9'; i++)
{
if (i != 2 && != 4)
{
putchar(c);
}
}
putchar('\n');
}
